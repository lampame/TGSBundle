{
  "name": "tgsbundle",
  "version": "1.0.0",
  "private": true,
  "description": "Automated GramJS browser bundle for Telegram plugin usage",
  "scripts": {
    "build": "echo 'Build is handled by GitHub Actions — push a tag to trigger.'"
  }
}
